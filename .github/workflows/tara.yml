name: Depo tara

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  tara:
    runs-on: ubuntu-latest
    steps:
      - name: Sekiz sayfa ilerle
        env:
          SCAN_URL: ${{ secrets.SCAN_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$SCAN_URL" ] || [ -z "$CRON_SECRET" ]; then
            echo "GitHub secret eksik: SCAN_URL ve CRON_SECRET"
            exit 1
          fi
          base="${SCAN_URL%/}"
          for i in 1 2 3 4 5 6 7 8; do
            body=$(curl -fsS -m 55 -H "Authorization: Bearer $CRON_SECRET" "$base/api/cron/scan")
            echo "$body"
            echo "$body" | grep -q '"blocked":true' && exit 0
            sleep 3
          done
